#!/bin/sh

BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3

echo "building the web project"

cd "$BUILD_DIR"

yarn web:build

mv packages/web/build/ "$2/temp"
rm -rf .
mv "$2/tmp/build/" .

ls

npm init -y

npm install express --save

exit 0