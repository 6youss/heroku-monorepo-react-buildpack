#!/bin/sh

BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3

echo "building the web project"

cd "$BUILD_DIR"

yarn web:build

mv packages/web/build/* $2/web-build/
rm -rf *
mv $2/web-build/* .
mv Procfile /app/

ls

npm init -y

npm install express --save

exit 0